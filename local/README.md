# Local private configurations

Files in this directory may contain private node addresses, UUIDs, Reality
keys, and subscription tokens. Only this README and `.gitignore` may be
tracked. Never upload the generated YAML files.
